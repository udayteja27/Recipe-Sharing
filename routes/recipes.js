// routes/recipes.js

const express = require('express');
const router = express.Router();
const Recipe = require('../models/Recipe');

// CRUD operations for recipes will go here

module.exports = router;
